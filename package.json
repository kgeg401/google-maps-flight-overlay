{
  "name": "google-maps-flight-overlay",
  "version": "0.10.0",
  "private": true,
  "type": "module",
  "description": "Tampermonkey userscript that overlays live aircraft markers on Google Maps.",
  "scripts": {
    "build": "node scripts/build-userscript.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
